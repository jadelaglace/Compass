# Review?????????????#187?

> ???2026-07-10 ? ?? issue?#187 ? ???fix/187-trigger-wiring

## ??

T1.3 ? `Trigger` ???????????? + ??????**??????????**??`cargo build --release` ? 6 ??? dead_code warning?`Trigger` enum?`apply_trigger`?`apply_trigger_if_eligible`?`in_cooldown`?`parse_ts`?`Trigger::deltas`/`cooldown_days`?`db.last_trigger_time`???? `AccessDepth`?PATCH /access????5 ? Trigger ??????

## ??

| ?? | ?? |
|------|------|
| `frontmatter.rs` | ?? `pub fn extract_refs(body) -> Vec<String>`?`[[id]]` ???watcher ? /graph ??? |
| `api.rs` | `extract_refs(path)` ??????? `frontmatter::extract_refs`?????? |
| `watcher.rs` | `process_single_file` upsert ??? `fire_link_and_cited_triggers` |

### ?????`fire_link_and_cited_triggers`?

- ????? `[[refs]]`?
- **Linked**??????`apply_trigger_if_eligible(Linked)`?interest +1?7 ????????? frontmatter + upsert + ? score_history?
- **Cited**??????????????????? frontmatter????? weights?-> `apply_trigger_if_eligible(Cited)`?consensus +2?1 ??? -> ???? frontmatter + upsert + ???
- ??? `db.last_trigger_time` per-type + `score_history` ??????????????????
- ???? frontmatter `score:` ??body/content_hash ???

### ?????????

- **???????**?`rebuild_from_vault` ??????????????????? backfill ????
- **ManualMark / CaseAdded / ReviewCompleted ??**?????????frontmatter tag/flag ??????????????????? variant ?? enum ??????? dead_code?enum ?? Linked/Cited ????
- ?? db ???? I/O??? vault ?????????????????

## ??

- `cargo test`?**117 passed / 0 failed**?+2 ???
  - `test_trigger_linked_and_cited_fire_once`?A ?? B -> A interest 50?51?B consensus 50?52?score_history ????B frontmatter ????????????
  - `test_trigger_cited_skips_nonexistent_target`????????????Linked ????
- `cargo build --release`????????? dead_code **????**?11?5 warning???????????Layer enum / open_in_memory / api.rs unused var??
- `cargo fmt --check`????
- `cargo clippy --all-targets`?? error???? warning?

## ????

- ?? 115 ????????????/??/frontmatter ???????? + extract_refs ??????
- ????????????? FileWatcher ?????????????????
